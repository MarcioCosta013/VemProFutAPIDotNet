# VemProFutAPIDotNet
Uma API REST do Projeto VemProFutFullstack só que feita em .NET usando o C# como linguagem e aplicando estrutura Clean Architecture/DDD o padrão mais adotado em projetos médios/grandes no mercado .NET atual.
